-# coding: utf-8
%h1 Enter Skill Tracking Information for #{@kid.name} for #{@this_track.grading_period.name}
%form{:action => "/skill_tracks/update/#{@this_track.id}", :method => "POST"}
	%div.float
		%p Homeroom Teacher
		%select{:name => "form_data[homeroom_teacher]"}
			%option{:value => ""}
			- @teachers.each do |t|
				-if @this_track.teacher.nil? == false && (@this_track.teacher.id == t.id)
					%option{:value => "#{@this_track.teacher.id}", :selected => 'selected'} #{Teacher.find_by(:id => @this_track.teacher).name}
				-else
					%option{:value => "#{t.id}"} #{t.name}
		%p Student Grade Level
		%select{:name => "form_data[student_level]"}
			- @student_levels.each do |l|
				-if @this_track.student_level.nil? == false && (@this_track.student_level == l)
					%option{:value => "#{l}", :selected => 'selected'} #{l}
				-else
					%option{:value => "#{l}"} #{l}
		%p EAL / SSS Status
		%select{:name => "form_data[eal_sss_status]"}
			- @eal_sss_status.each do |s|
				-if @this_track.eal_sss_status.nil? == false && (@this_track.eal_sss_status == s)
					%option{:value => "#{s}", :selected => 'selected'} #{s}
				-else
					%option{:value => "#{s}"} #{s}
		%hr
		%p Language Arts, Social Studies, Science
		%select{:name => "form_data[language_arts_teacher]"}
			%option{:value => ""}
			- @teachers.each do |t|
				-if @this_track.language_arts_teacher.nil? == false && (@this_track.language_arts_teacher == t.id.to_s)
					%option{:value => "#{@this_track.language_arts_teacher}", :selected => 'selected'} #{Teacher.find_by(:id => @this_track.language_arts_teacher).name}
				-else
					%option{:value => "#{t.id}"} #{t.name}
		%select{:name => "form_data[language_arts_level]"}
			%option{:value => ""}
			- @levels.each do |l|
				-if @this_track.language_arts_level.nil? == false && (@this_track.language_arts_level == l)
					%option{:value => "#{l}", :selected => 'selected'} #{l}
				-else
					%option{:value => "#{l}"} #{l}

		%p Math
		%select{:name => "form_data[math_teacher]"}
			%option{:value => ""}
			- @teachers.each do |t|
				-if @this_track.math_teacher.nil? == false && (@this_track.math_teacher == t.id.to_s)
					%option{:value => "#{@this_track.math_teacher}", :selected => 'selected'} #{Teacher.find_by(:id => @this_track.math_teacher).name}
				-else
					%option{:value => "#{t.id}"} #{t.name}
		%select{:name => "form_data[math_level]"}
			%option{:value => ""}
			- @levels.each do |l|
				-if @this_track.math_level.nil? == false && (@this_track.math_level == l)
					%option{:value => "#{l}", :selected => 'selected'} #{l}
				-else
					%option{:value => "#{l}"} #{l}
			
		%p Work & Study Habits / Citizenship
		%select{:name => "form_data[work_study_teacher]"}
			%option{:value => ""}
			- @teachers.each do |t|
				-if @this_track.work_study_teacher.nil? == false && (@this_track.work_study_teacher == t.id.to_s)
					%option{:value => "#{@this_track.work_study_teacher}", :selected => 'selected'} #{Teacher.find_by(:id => @this_track.work_study_teacher).name}
				-else
					%option{:value => "#{t.id}"} #{t.name}
			
		%select{:name => "form_data[work_study_level]"}
			%option{:value => ""}
			- @levels.each do |l|
				-if @this_track.work_study_level.nil? == false && (@this_track.work_study_level == l)
					%option{:value => "#{l}", :selected => 'selected'} #{l}
				-else
					%option{:value => "#{l}"} #{l}

	%div.float
		%p Art
		%select{:name => "form_data[art_teacher]"}
			%option{:value => ""}
			- @teachers.each do |t|
				-if @this_track.art_teacher.nil? == false && (@this_track.art_teacher == t.id.to_s)
					%option{:value => "#{@this_track.art_teacher}", :selected => 'selected'} #{Teacher.find_by(:id => @this_track.art_teacher).name}
				-else
					%option{:value => "#{t.id}"} #{t.name}
		%select{:name => "form_data[art_level]"}
			%option{:value => ""}
			- @levels.each do |l|
				-if @this_track.art_level.nil? == false && (@this_track.art_level == l)
					%option{:value => "#{l}", :selected => 'selected'} #{l}
				-else
					%option{:value => "#{l}"} #{l}

		%p Information Communication Technology
		%select{:name => "form_data[ict_teacher]"}
			%option{:value => ""}
			- @teachers.each do |t|
				-if @this_track.ict_teacher.nil? == false && (@this_track.ict_teacher == t.id.to_s)
					%option{:value => "#{@this_track.ict_teacher}", :selected => 'selected'} #{Teacher.find_by(:id => @this_track.ict_teacher).name}
				-else
					%option{:value => "#{t.id}"} #{t.name}
		%select{:name => "form_data[ict_level]"}
			%option{:value => ""}
			- @levels.each do |l|
				-if @this_track.ict_level.nil? == false && (@this_track.ict_level == l)
					%option{:value => "#{l}", :selected => 'selected'} #{l}
				-else
					%option{:value => "#{l}"} #{l}

		%p Music
		%select{:name => "form_data[music_teacher]"}
			%option{:value => ""}
			- @teachers.each do |t|
				-if @this_track.music_teacher.nil? == false && (@this_track.music_teacher == t.id.to_s)
					%option{:value => "#{@this_track.music_teacher}", :selected => 'selected'} #{Teacher.find_by(:id => @this_track.music_teacher).name}
				-else
					%option{:value => "#{t.id}"} #{t.name}
		%select{:name => "form_data[music_level]"}
			%option{:value => ""}
			- @levels.each do |l|
				-if @this_track.music_level.nil? == false && (@this_track.music_level == l)
					%option{:value => "#{l}", :selected => 'selected'} #{l}
				-else
					%option{:value => "#{l}"} #{l}

		%p Physical Education
		%select{:name => "form_data[pe_teacher]"}
			%option{:value => ""}
			- @teachers.each do |t|
				-if @this_track.pe_teacher.nil? == false && (@this_track.pe_teacher == t.id.to_s)
					%option{:value => "#{@this_track.pe_teacher}", :selected => 'selected'} #{Teacher.find_by(:id => @this_track.pe_teacher).name}
				-else
					%option{:value => "#{t.id}"} #{t.name}
		%select{:name => "form_data[pe_level]"}
			%option{:value => ""}
			- @levels.each do |l|
				-if @this_track.pe_level.nil? == false && (@this_track.pe_level == l)
					%option{:value => "#{l}", :selected => 'selected'} #{l}
				-else
					%option{:value => "#{l}"} #{l}

		%p Thai Language
		%select{:name => "form_data[thai_teacher]"}
			%option{:value => ""}
			- @teachers.each do |t|
				-if @this_track.thai_teacher.nil? == false && (@this_track.thai_teacher == t.id.to_s)
					%option{:value => "#{@this_track.thai_teacher}", :selected => 'selected'} #{Teacher.find_by(:id => @this_track.thai_teacher).name}
				-else
					%option{:value => "#{t.id}"} #{t.name}
		%select{:name => "form_data[thai_level]"}
			%option{:value => ""}
			- @levels.each do |l|
				-if @this_track.thai_level.nil? == false && (@this_track.thai_level == l)
					%option{:value => "#{l}", :selected => 'selected'} #{l}
				-else
					%option{:value => "#{l}"} #{l}
		%p Native / Non-Native Thai Status
		%select{:name => "form_data[thai_native]"}
			- @thai_native.each do |n|
				-if @this_track.thai_native.nil? == false && (@this_track.thai_native == n)
					%option{:value => "#{n}", :selected => 'selected'} #{n}
				-else
					%option{:value => "#{n}"} #{n}

		%br				
		%input.button{:type => "submit", :value => "Update Skill Tracking", :title => "Update Skill Tracking", :class => "button"}
