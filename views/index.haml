%h2 Students
%ul.students
	- @students.each do |s|
		= partial( :"/listing_student", {:student => s } )
%h2 Teachers
%ul.teachers
	- @teachers.each do |t|
		= partial( :"/listing_teacher", {:teacher => t } )
