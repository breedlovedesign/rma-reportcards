-# coding: utf-8
%li{:class => "list_row"}
	%ul
	- @language_arts.each do |la|
		%hr
		%li{:class => "list_row"}= la[:student_name].ljust(28, ".") 
		%label{:class => "collapse", :for => "la_prev_comments"}
			Show/Hide
		%ul{:id => "la_prev_comments"}
			-la[:comment_object].each do |comment|
				%li{:class => "list_row",}= comment.texto			
